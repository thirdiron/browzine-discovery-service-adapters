machine:
  node:
    version: 4.2.6
  npm:
    version: 2.14.12

dependencies:
  post:
    - npm run compile-ci

deployment:
  hotfix:
    branch: /hotfix\/.*/
    commands:
      - npm run compile-ci
  feature:
    branch: /feature\/.*/
    commands:
      - npm run compile-ci
  develop:
    branch: develop
    commands:
      - npm run compile-ci
  master:
    branch: master
    commands:
      - npm run compile-ci
